diff -Nru zgv-5.2/config.mk zgv-5.2.new/config.mk
--- zgv-5.2/config.mk	Sat Nov  4 15:55:35 2000
+++ zgv-5.2.new/config.mk	Sat Nov  4 15:54:39 2000
@@ -1,4 +1,4 @@
-# This file configures zgv's Makefiles. You should edit the settings
+# tHIS FIle configures zgv's Makefiles. You should edit the settings
 # below as needed.
 
 # If you want to tune the optimisation used (you shouldn't need to,
@@ -34,7 +34,7 @@
 #
 # If you don't know what to do, leave it as-is.
 #
-#INFO_DIR_UPDATE=no
+INFO_DIR_UPDATE=no
 
 
 # ------------------- Format-related options ----------------------
diff -Nru zgv-5.2/doc/Makefile zgv-5.2.new/doc/Makefile
--- zgv-5.2/doc/Makefile	Sat Nov  4 15:55:35 2000
+++ zgv-5.2.new/doc/Makefile	Sat Nov  4 15:54:39 2000
@@ -34,8 +34,10 @@
 else
 install: ../src/install-info
 endif
-	install -m 444 zgv.1 $(MANDIR)
-	install -m 444 zgv zgv-? $(INFODIR)
+	mkdir -p $(DESTDIR)$(MANDIR)
+	mkdir -p $(DESTDIR)$(INFODIR)
+	install zgv.1 $(DESTDIR)$(MANDIR)
+	install zgv zgv-? $(DESTDIR)$(INFODIR)
 # Update info `dir' file.
 # Info always uses a dir file in preference to a dir.gz, so we don't use
 # dir.gz unless it's the only game in town.
diff -Nru zgv-5.2/src/Makefile zgv-5.2.new/src/Makefile
--- zgv-5.2/src/Makefile	Sat Nov  4 15:55:35 2000
+++ zgv-5.2.new/src/Makefile	Sat Nov  4 15:57:00 2000
@@ -13,11 +13,12 @@
 CC=gcc
 
 WARNOPTS = -Wall
+OPTFLAGS = -O2 -fomit-frame-pointer -finline-functions
 
 CFLAGS = $(WARNOPTS) $(PCDDEF) \
 	-DRGB_DB_FILE=\"$(RGB_DB)\" \
 	-DTIFFTOPNM=\"$(TIFFTOPNM)\" \
-	-O2 -fomit-frame-pointer -finline-functions
+	$(OPTFLAGS)
 
 ZGV_LIBS = -lvgagl -lvga -ljpeg -lpng -lz -lm
 ifeq ($(PCDDEF),-DPCD_SUPPORT)
@@ -53,7 +54,8 @@
 	$(CC) $(INFODIRDEF) -o install-info install-info.c
 
 install: all
-	install -m 4511 -o root -g root -s zgv $(BINDIR)
+	mkdir -p $(DESTDIR)$(BINDIR)
+	install -s zgv $(DESTDIR)$(BINDIR)
 
 uninstall:
 	$(RM) $(BINDIR)/zgv
